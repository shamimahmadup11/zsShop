// Define backend domain
const backendDomain = "http://localhost:4000";

// Define the signup API details
const signupApi = {
  url: `${backendDomain}/api/signup`,
  method: "post",
};

// Export the API details
export default signupApi;
